Eddroid.Main = function(game){
    this.map;
    this.mapj;
    this.blockedLayer;

};
 Eddroid.Main.prototype = {

   preload: function() {
   },

   create: function(){
     this.world.setBounds(0, 0, 800, 600);
     this.physics.startSystem(Phaser.Physics.ARCADE);
     map = this.add.sprite(0, 0, 'map');
     mapj = this.add.tilemap('mapj');



   },
 };
